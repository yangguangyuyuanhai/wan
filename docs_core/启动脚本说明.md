# 启动脚本说明

## 📋 概述

所有启动脚本已配置为自动使用 **label_studio** Conda环境。

**Conda路径**: `C:\Users\YRQ\miniconda3\_conda.exe`  
**环境名称**: `label_studio`

---

## 🚀 快速开始

### 1️⃣ 首次使用（必须）

双击运行：
```
安装依赖.bat
```

这将自动：
- 检查/创建 label_studio 环境
- 安装所有必需的依赖包
- 验证安装是否成功

### 2️⃣ 检查环境（推荐）

双击运行：
```
检查环境.bat
```

这将检查：
- ✓ Conda是否安装
- ✓ label_studio环境是否存在
- ✓ Python版本
- ✓ PyQt5是否安装
- ✓ OpenCV是否安装

### 3️⃣ 启动系统

双击运行：
```
一键启动所有版本.bat
```

选择要启动的版本：
- **[1] Qt GUI界面版本** - 推荐，图形界面
- **[2] 同步版本** - 命令行版本
- **[3] 异步版本** - 多相机并发
- **[4] 运行系统测试** - 测试所有模块
- **[5] 退出**

---

## 📁 所有启动脚本

### 主目录 (service_new/)

| 脚本名称 | 功能 | 说明 |
|---------|------|------|
| `安装依赖.bat` | 安装所有依赖 | 首次使用必须运行 |
| `检查环境.bat` | 检查环境配置 | 验证环境是否正确 |
| `一键启动所有版本.bat` | 启动菜单 | 选择要启动的版本 |
| `启动同步版本.bat` | 启动同步版本 | 命令行版本 |
| `快速测试.bat` | 运行测试 | 测试所有模块 |

### Qt界面 (service_new/service_qt/)

| 脚本名称 | 功能 | 说明 |
|---------|------|------|
| `启动GUI.bat` | 启动Qt界面 | 图形界面版本 |

### 异步版本 (service_new/service_asyncio/)

| 脚本名称 | 功能 | 说明 |
|---------|------|------|
| `启动异步版本.bat` | 启动异步版本 | 多相机并发 |

---

## 🔧 使用流程

### 完整流程

```
1. 安装依赖.bat
   ↓
2. 检查环境.bat (验证)
   ↓
3. 一键启动所有版本.bat
   ↓
4. 选择版本启动
```

### 快速流程（已安装依赖）

```
一键启动所有版本.bat → 选择版本
```

---

## 💡 各版本特点

### Qt GUI界面版本 ⭐ 推荐

**启动**: `service_qt/启动GUI.bat`

**特点**:
- ✅ 图形界面，操作直观
- ✅ 海康威视配色风格
- ✅ 实时参数调整
- ✅ 检测结果可视化
- ✅ 性能监控

**适合**: 日常使用、演示、调试

### 同步版本

**启动**: `启动同步版本.bat`

**特点**:
- ✅ 命令行界面
- ✅ 单相机处理
- ✅ 稳定可靠
- ✅ 资源占用低

**适合**: 服务器部署、自动化任务

### 异步版本

**启动**: `service_asyncio/启动异步版本.bat`

**特点**:
- ✅ 多相机并发
- ✅ 高性能处理
- ✅ 异步架构
- ✅ 3-5倍吞吐量提升

**适合**: 多相机场景、高性能需求

---

## ⚙️ 环境配置

### Conda环境信息

```
环境名称: label_studio
Python版本: 3.9.23
PyQt5版本: 5.15.11
```

### 必需依赖

- PyQt5 >= 5.15.0
- PyQt5-tools >= 5.15.0
- opencv-python >= 4.5.0
- numpy >= 1.19.0
- pillow

### 可选依赖

- ultralytics (YOLO检测)

---

## 🐛 常见问题

### Q1: 双击.bat没反应？

**A**: 检查Conda路径是否正确：
```
C:\Users\YRQ\miniconda3\_conda.exe
```

如果路径不同，需要修改所有.bat文件中的路径。

### Q2: 提示环境不存在？

**A**: 运行 `安装依赖.bat`，会自动创建环境。

### Q3: 提示依赖缺失？

**A**: 运行 `安装依赖.bat` 重新安装所有依赖。

### Q4: 如何修改Conda路径？

**A**: 编辑.bat文件，将所有：
```batch
C:\Users\YRQ\miniconda3\_conda.exe
```
替换为你的Conda路径。

### Q5: 如何使用其他环境？

**A**: 编辑.bat文件，将所有：
```batch
-n label_studio
```
替换为你的环境名称。

---

## 📝 手动命令

如果不想用.bat脚本，可以手动运行：

### 启动Qt界面
```bash
cd service_new/service_qt
C:\Users\YRQ\miniconda3\_conda.exe run -n label_studio python run_gui.py
```

### 启动同步版本
```bash
cd service_new
C:\Users\YRQ\miniconda3\_conda.exe run -n label_studio python main.py
```

### 启动异步版本
```bash
cd service_new/service_asyncio
C:\Users\YRQ\miniconda3\_conda.exe run -n label_studio python main_async.py
```

### 运行测试
```bash
cd service_new
C:\Users\YRQ\miniconda3\_conda.exe run -n label_studio python test_system.py
```

---

## 🎯 推荐使用方式

### 日常使用

```
一键启动所有版本.bat → [1] Qt GUI界面版本
```

### 开发调试

```
检查环境.bat → 快速测试.bat → 启动对应版本
```

### 生产部署

```
启动同步版本.bat (或 启动异步版本.bat)
```

---

## 📞 技术支持

遇到问题？

1. 运行 `检查环境.bat` 查看环境状态
2. 运行 `快速测试.bat` 测试系统
3. 查看 `测试报告.md` 了解系统状态
4. 查看各目录下的README.md文档

---

**最后更新**: 2025-01-29  
**版本**: 1.0  
**作者**: Kiro AI Assistant
